import actionNames from '../../actions/actionNames';

export const packages = (state = [], action) => {
    let newState = [];

    switch (action.type) {
        default:
            return state;
    }
};